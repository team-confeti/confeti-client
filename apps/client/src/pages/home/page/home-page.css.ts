import { style } from '@vanilla-extract/css';

export const container = style({
  position: 'relative',
});

export const navTabsWrapper = style({
  position: 'absolute',
  top: '5.4rem',
  left: 0,
  right: 0,
  zIndex: 15,
});
