import { type ReactNode } from 'react';

import { cn } from '../../utils';

import { {{camelCase name}}Variants } from './{{kebabCase name}}.css';

interface Props {
  children: ReactNode;
  className?: string;
}

const {{pascalCase name}} = ({ className, children, ...props }: Props) => (
  <div className={cn({{camelCase name}}Variants(), className)} {...props}>
    {children}
  </div>
);

export default {{pascalCase name}};